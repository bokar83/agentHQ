# Engagements

Per-client engagement folders live here. One folder per client (lowercase shortname). The engagement-ops skill creates and maintains these.

Internal projects do not live here :  they live in ../roadmap/.
