# Session Handoff - Blotato 201 Fix + Test Infrastructure - 2026-04-27

## TL;DR

Auto-publisher failed at 07:01 MT because Blotato returned HTTP 201 (not 200) on a
successful POST. The post was live on X the whole time. Fixed, Notion record repaired,
370/370 tests passing, deployed to VPS at commit `70aa317`.

## What was built / changed

- `orchestrator/blotato_publisher.py`: all three HTTP status checks changed from `!= 200`
  to `not in (200, 201)`. Blotato uses 200 for some endpoints and 201 for others.
- `orchestrator/skills/doc_routing/doc_routing_crew.py`: fixed 3 bare `from doc_routing.*`
  imports to `from skills.doc_routing.*` (consistent with rest of codebase).
- `orchestrator/conftest.py` (new): inserts both agentsHQ root and orchestrator/ into
  sys.path so the `skills` namespace package resolves across both locations in all tests.
- `orchestrator/pytest.ini` (new): `pythonpath = .` so pytest adds orchestrator/ to path.
- `orchestrator/tests/test_doc_routing/conftest.py` (new): secondary path insert for
  the doc_routing test directory.
- `orchestrator/tests/test_doc_routing/__init__.py` (deleted): was forcing package-mode
  import which blocked namespace package resolution.
- `orchestrator/tests/test_blotato_publisher.py`: new test `test_publish_http_201_accepted_as_success`.
- `orchestrator/tests/test_signal_works.py`: fixed stale assertion. Added
  `fetchone.return_value = None` to force INSERT path so `signal_works` literal appears
  in the params tuple.

## Manual repair done

Notion record `341bcf1a` ("One constraint nobody has named yet"):
- Status: `Posted` (was `PublishFailed`)
- X Posted URL: `https://x.com/boubacarbarry/status/2048749476999811174`
- Submission ID: `555b3507-0e44-41a1-8886-c18c35a5686b`
- Source Note updated to explain the manual repair

## Decisions made

1. **Blotato returns 201, not 200, for POST /v2/posts.** API docs say 200 but production
   returns 201. Accept both. The GET poll endpoint correctly returns 200 (no change needed).
2. **Root cause of doc_routing test failures was a phantom `skills/__init__.py`** at
   `agentsHQ/skills/` (accidentally created in a prior session) shadowing the namespace
   package and blocking `skills.forge_cli` lookups. Deleted. The `skills` package is a
   namespace package spanning `agentsHQ/skills/` (user-facing skills) and
   `orchestrator/skills/` (Python skills used by crews). No `__init__.py` should exist
   in either parent.
3. **Test count: 370/370** (up from 353 before this session; 17 previously-broken
   doc_routing tests now pass).

## What is NOT done

- NLM export cron: fires 06:00 UTC. First run was tonight. Not yet checked.
  Log: `/var/log/nlm_registry_export.log` on VPS.
- M9c (cross-session memory): gated on 1 week of M9b usage. Earliest: 2026-05-03.
- M5 (Chairman / L5 Learning): gate opens 2026-05-08 (14 days of task_outcomes data).

## Open questions

None blocking.

## Next session must start here

1. `ssh root@72.60.209.109 "cat /var/log/nlm_registry_export.log"` to confirm NLM cron ran.
2. Check docker logs for any auto-publisher failures since the fix.
3. M9c or M5 depending on date (see roadmap).

## Files changed this session

```
orchestrator/blotato_publisher.py               : 201 accepted as success
orchestrator/skills/doc_routing/doc_routing_crew.py : fixed bare imports
orchestrator/conftest.py                        : NEW: dual sys.path insert
orchestrator/pytest.ini                         : NEW: pythonpath = .
orchestrator/tests/test_doc_routing/conftest.py : NEW: path insert for test pkg
orchestrator/tests/test_doc_routing/__init__.py : DELETED
orchestrator/tests/test_blotato_publisher.py    : new 201 test
orchestrator/tests/test_signal_works.py         : fixed stale assertion
docs/roadmap/atlas.md                           : session log appended
docs/handoff/2026-04-27-blotato-201-fix.md      : this file
```
