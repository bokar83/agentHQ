# Session Handoff: M11b Capability Routing + Sankofa Council: 2026-04-26

## TL;DR

Built and shipped M11b: replaced ROLE_MODEL (4 hardcoded Anthropic aliases) with ROLE_CAPABILITY
(capability tag + cost ceiling per role/complexity) in orchestrator/agents.py. select_llm() now
delegates to select_by_capability() across all 18 models in COUNCIL_MODEL_REGISTRY. Merged to
main, pushed to origin, rebuilt container, verified live. Ran Sankofa Council on the routing
decisions and A/B test design. Council surfaced two action items; both were done by the M10 agent
in parallel. Option A (A/B comparison script for leGriot) is the next task.

## What was built / changed

- `orchestrator/agents.py`: deleted ROLE_MODEL dict, added ROLE_CAPABILITY dict (capability +
  cost ceiling per role/complexity), rewrote select_llm() to call select_by_capability(),
  kept ROLE_TEMPERATURE unchanged. Added _validate_role_capability_dict() import-time check
  (done by M10 agent). consultant/complex ceiling raised from "high" to "medium-high" (done by
  M10 agent).
- `docs/roadmap/atlas.md`: M11b status updated to SHIPPED, session log entry added.
- `docs/reference/legriot-quality-rubric.md`: written by M10 agent (commit 6c55ef9).
  5 criteria: hook strength, voice fidelity, diagnosis clarity, AI slop absence, CTA sharpness.
- Branch feat/m11b-capability-routing merged to main at a40713d.
- VPS: container rebuilt via docker compose up -d --build orchestrator, verified healthy.
- Memory: project_atlas_m9_m11_state.md updated. Two new feedback files written.

## Decisions made

- ROLE_CAPABILITY architecture is correct; data layer (quality harness) is absent but the
  container is valid. A/B test is the validation step, not a nice-to-have.
- Cost-first tie-breaking is right for infrastructure roles (qa, planner). Wrong for brand-risk
  roles. consultant/complex ceiling raised to medium-high to enforce Sonnet-class minimum.
- social/moderate stays on Grok-4 (creative_divergence). Brand risk is gated by human review
  before any post goes live. This is a calculated bet, not confirmed by data yet.
- The leGriot quality rubric must be written BEFORE the A/B test runs, not after. Done.
- "DO NOT TOUCH" flags in memory are paternalistic overreach. Boubacar coordinates agents.
  Memory should say "ready to build, another agent may pick this up": nothing stronger.

## What is NOT done

- Option A A/B comparison script: not yet built. Prereqs (rubric + consultant ceiling fix) are
  done. Ready to build.
- M11d (weekly model review agent): queued after Option A validates routing.
- M9b (web chat native panel): queued after M9a smoke test confirmed on VPS.
- M11c (research engine rewrite): queued after M11b.
- VPS: container has M11b live but consultant/complex ceiling fix (agents.py unstaged on
  feat/atlas-m10-crew-contract) is not yet deployed. That agent must commit + rebuild.

## Open questions

- Has the M10 agent committed and pushed the agents.py consultant/complex fix? Check
  orchestrator/agents.py on main before assuming it's live on VPS.
- Monday 2026-04-27 07:00 MT: did X auto-publish fire "One constraint nobody has named yet"?
  Check VPS logs and Notion Status before any new milestone work.

## Next session must start here

1. Check if M10 agent committed the consultant/complex ceiling fix to main and deployed to VPS:
   `grep -A2 "consultant.*complex" orchestrator/agents.py` should show "medium-high"
2. Verify Monday auto-publish fired: `ssh root@72.60.209.109 'docker logs orc-crewai --since 24h | grep -E "auto_publisher|PublishFailed|Posted"'`
3. Build Option A A/B comparison script (~45 min):
   - Script in orchestrator/ (or scripts/) calls leGriot social crew on 3 seed ideas
   - Runs against Grok-4, Claude Sonnet, Mistral-Large
   - Saves drafts side-by-side to workspace/ with no model labels visible
   - Boubacar scores blind using docs/reference/legriot-quality-rubric.md
4. After scoring: update ROLE_CAPABILITY social/moderate based on results, commit, redeploy

## Files changed this session

```
orchestrator/agents.py         : ROLE_MODEL -> ROLE_CAPABILITY, select_llm() rewritten
docs/roadmap/atlas.md          : M11b SHIPPED, session log, conflict resolved
docs/handoff/2026-04-26-m11b-shutdown.md : this file
memory/project_atlas_m9_m11_state.md     : updated (both items done)
memory/feedback_no_paternalistic_gates.md: new
memory/feedback_cost_floor_brand_risk_roles.md: new
memory/MEMORY.md               : 3 pointer updates
```
