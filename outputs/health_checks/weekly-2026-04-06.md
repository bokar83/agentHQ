# agentsHQ Weekly Health Check — 2026-04-06

## Status: YELLOW
- GREEN = no issues found
- YELLOW = minor issues (dead code, missing docs) — not urgent
- RED = broken routing, missing deps, or logic bugs — needs fixing this week

## Issues Found

### CONFIG DRIFT — env vars referenced in code but absent from docker-compose.yml

- **`tools.py:216,272`** | `N8N_ESCALATION_WEBHOOK` | YELLOW | Referenced via `os.environ.get("N8N_ESCALATION_WEBHOOK")` with no default and no entry in docker-compose.yml. Code guards with `if webhook_url:` so escalation webhook silently does nothing in production. **Fix:** Add `N8N_ESCALATION_WEBHOOK=${N8N_ESCALATION_WEBHOOK}` to docker-compose.yml orchestrator environment block, or confirm this feature is intentionally disabled.

- **`orchestrator.py:860`** | `ALLOWED_USER_IDS` | YELLOW | `os.environ.get("ALLOWED_USER_IDS", "")` — empty default means user ID filtering is effectively inactive (no IDs whitelisted). Not in docker-compose.yml. **Fix:** Add `ALLOWED_USER_IDS=${ALLOWED_USER_IDS}` to docker-compose.yml and confirm intent (open access vs. restricted).

- **`orchestrator.py:52,189,314,521,598,1198`** | `AGENTS_LOG_DIR` / `AGENTS_OUTPUT_DIR` | YELLOW | Both have safe defaults (`/app/logs`, `/app/outputs`) and these paths are volume-mapped in docker-compose.yml, so production is fine. **Fix (optional):** Add explicit declarations for clarity.

- **`council.py:37,38`** | `COUNCIL_CONVERGENCE_THRESHOLD` / `COUNCIL_MAX_ROUNDS` | YELLOW | Both have sensible defaults (0.90, 3). Not declared in docker-compose.yml but pose no runtime risk. **Fix (optional):** Add to docker-compose.yml for explicit tuning.

- **`notifier.py:28,29`** | `SMTP_HOST` / `SMTP_PORT` | YELLOW | Both have correct defaults (`smtp.gmail.com`, `587`). Not in docker-compose.yml but functionally safe. **Fix (optional):** Add for explicitness.

- **`orchestrator.py:794`** | `DISABLE_EVOLUTION` | YELLOW | No default; code checks `== "true"` so absence = feature enabled. Not in docker-compose.yml. **Fix (optional):** Add `DISABLE_EVOLUTION=false` to docker-compose.yml for explicit control.

### REQUIREMENTS DRIFT — packages in requirements.txt with no direct imports

- **`requirements.txt`** | `litellm` | YELLOW | Listed in requirements.txt but never directly imported in any orchestrator .py file. Likely a transitive dependency of `crewai`. **Fix:** Confirm crewai dependency — if so, add a comment noting it is a transitive dep, or remove if crewai no longer needs it.

- **`requirements.txt`** | `tenacity` | YELLOW | Listed in requirements.txt but never directly imported. Likely transitive via crewai or crewai-tools. **Fix:** Same as litellm — add comment or remove.

- **`requirements.txt`** | `mcp` | YELLOW | Listed in requirements.txt. Used indirectly via `crewai_tools.MCPServerAdapter` (tools.py:436). Not a direct import. **Fix:** Add comment noting this is a transitive dep for MCPServerAdapter.

## Checks Passed

- **Router/Registry Match**: All 13 task types in `router.py` with a non-None crew value map to a valid entry in `CREW_REGISTRY`. `unknown_crew` in registry is an intentional fallback — not a mismatch.
- **Orphaned Files**: All 11 `.py` files in `orchestrator/` are either in the expected core list or imported by another file. No orphaned files.
- **Dead Agents**: All 19 `build_*_agent()` functions defined in `agents.py` are called by at least one crew in `crews.py`. (`build_news_brief_agent` is imported locally inside `build_news_brief_crew()` at line 1356 rather than at module level — functional, not dead.)
- **Critical Dependencies Present**: All packages required for runtime operation (`crewai`, `crewai-tools`, `fastapi`, `uvicorn`, `httpx`, `pydantic`, `openai`, `qdrant-client`, `psycopg2-binary`, `firecrawl-py`, `PyGithub`, `google-api-python-client`, `google-auth`, `requests`, `pytz`) are in requirements.txt and match imports found in code.

---
Report ends here.
