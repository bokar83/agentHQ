# agentsHQ Weekly Health Check — 2026-05-11

## Status: YELLOW
Minor issues only — dead code, misleading registry entries, a few missing env vars. No broken routing, no missing core deps. Not urgent.

---

## Issues Found

### Check 1 — Router/Registry Mismatch

**[YELLOW]** `router.py` — task type `"chat"` maps to `crew: "chat_crew"`, but `chat_crew` does not exist in `CREW_REGISTRY` (crews.py:3501).
- **Why it's not RED:** `app.py` short-circuits all chat requests before they reach `assemble_crew()` — lines 496–498 call `run_chat()` directly. The crew value in router.py is never consumed for this task type.
- **Recommended fix:** Change `router.py:234` `"crew": "chat_crew"` to `"crew": "chat_crew_bypass"` or a comment explaining the bypass, to avoid future confusion.

**[YELLOW]** `router.py` — task type `"inbound_lead"` maps to `crew: "inbound_lead_direct"`, but `inbound_lead_direct` does not exist in `CREW_REGISTRY` (crews.py:3501).
- **Why it's not RED:** The task description itself says "Not keyword-routed; invoked only by POST /inbound-lead." The webhook handler bypasses crew assembly entirely.
- **Recommended fix:** Same pattern — annotate or change the crew value to make the bypass explicit.

**[YELLOW]** 5 `CREW_REGISTRY` entries have no task type in router.py pointing to them. They are unreachable via the normal classification flow:
- `consulting_crew` (crews.py:3505) — no task type routes here
- `prompt_engineer_crew` (crews.py:3515) — no task type routes here
- `3d_website_crew` (crews.py:3516) — no task type routes here
- `news_brief_crew` (crews.py:3517) — no task type routes here
- `enrich_leads_crew` (crews.py:3524) — no task type routes here
- **Recommended fix:** Either add task types + keywords for these crews, or remove them from `CREW_REGISTRY` if they are invoked only via direct API calls (and document that).

---

### Check 2 — Orphaned Files

**[YELLOW]** `orchestrator/scrub_titles.py` — zero imports found in any other orchestrator file.
- **Recommended fix:** Verify if this is a standalone CLI script (safe to keep) or dead code (safe to delete).

---

### Check 3 — Requirements Drift

**[YELLOW]** `requirements.txt` — `notebooklm-mcp-cli>=0.5.0` is never imported as a Python module in any orchestrator/*.py file. It is used as a CLI binary (via `NLM_BIN` env var) invoked by subprocess in scheduler.py.
- **Severity:** Low — it's correctly installed and functional, just not a Python import.
- **Recommended fix:** Add a comment in requirements.txt: `# CLI tool only — not imported; invoked as subprocess via NLM_BIN`

**[YELLOW]** `requirements.txt` — `patchright>=1.49.0` is not directly imported in any orchestrator/*.py file (playwright is used in social_analytics.py instead).
- **Recommended fix:** Verify if patchright is a transitive dependency needed by another package, or if the direct import was removed. If unused, remove it.

All other packages in requirements.txt confirmed imported: crewai, fastapi, httpx, pydantic, openai, litellm, anthropic, qdrant-client, psycopg2-binary, firecrawl-py, beautifulsoup4, pypdf, youtube-transcript-api, playwright, pytz, requests, PyGithub, google-api-python-client, google-auth, tenacity, python-dotenv, mcp, pytest.

---

### Check 4 — Dead Agents

**Clean.** All 24 `build_*_agent()` functions defined in agents.py are called by at least one crew in crews.py (3 via lazy import: `build_news_brief_agent`, `build_gws_agent`, `build_notion_capture_agent`).

---

### Check 5 — Config Drift

The following env vars are referenced in orchestrator code but have **no entry** in docker-compose.yml's orchestrator environment block:

**[YELLOW]** `APIFY_API_TOKEN` — referenced in `niche_research.py:192,314`. Niche research feature silently disabled when absent. Not core, but undocumented.

**[YELLOW]** `FAL_API_KEY` — referenced in `upscale.py:19`. Image upscaling silently fails when absent.

**[YELLOW]** `NOTION_TASK_DB_ID` — referenced in code for Notion task queries. `notion_tasks_crew` may fail or return empty results without it.

**[YELLOW]** `CONTENT_DRIVE_FOLDER_ID` — referenced in several Drive publishing files. May conflict with or be an alias for `GOOGLE_DRIVE_FOLDER_ID` (which IS declared). Needs verification.

**[YELLOW]** `DATABASE_URL` — referenced in `griot_signal_brief.py:77` as an alternative DB connection string. Not declared in docker-compose; PostgreSQL connection uses individual POSTGRES_* vars instead. Low risk if griot_signal_brief falls back correctly.

**[YELLOW]** `VPS_HOST` + `VPS_SSH_KEY_PATH` — referenced for SSH operations. Not declared in docker-compose (only `VPS_IP` is). SSH-dependent features will fail silently.

Note: `SMTP_HOST` and `SMTP_PORT` have safe hardcoded defaults in `notifier.py:31-32` (`smtp.gmail.com:587`), so their absence from docker-compose is acceptable.

---

## Checks Passed

- **Router routing logic:** All 34 task types (except `chat` and `inbound_lead` noted above) have their crew value present in `CREW_REGISTRY`. No broken crew lookups for the active routing paths.
- **Orphaned files:** Only 1 file flagged (`scrub_titles.py`). All other non-standard files are imported.
- **Core requirements:** All primary Python dependencies confirmed imported and present in requirements.txt. No missing packages for core functionality.
- **Dead agents:** Zero. All 24 agent builders are wired into at least one crew.
- **Core env vars:** All critical runtime vars (OPENROUTER, SERPER, FIRECRAWL, ANTHROPIC, POSTGRES_*, QDRANT, TELEGRAM, NOTION_SECRET, SUPABASE_*, BLOTATO_*, BEEHIIV, GWS, KIE_AI, ORCHESTRATOR_API_KEY) are declared in docker-compose.yml.

---

*Report generated: 2026-05-11 | Checks: Router/Registry · Orphaned Files · Requirements Drift · Dead Agents · Config Drift*
