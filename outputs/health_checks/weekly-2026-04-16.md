# agentsHQ Weekly Health Check — 2026-04-16

## Status: RED
- RED = broken routing detected — 8 task types fall through to `build_unknown_crew` at runtime

---

## Issues Found

### CHECK 1 — ROUTER/REGISTRY MISMATCH (RED — Breaks Production Routing)

**8 task types in `router.py` point to crew keys that do NOT exist in `CREW_REGISTRY`.**
All 8 will silently fall back to `build_unknown_crew` when triggered.

| Task Type | router.py crew value | Nearest CREW_REGISTRY key |
|---|---|---|
| `website_build` | `web_builder_crew` | `website_crew` |
| `app_build` | `app_builder_crew` | `app_crew` |
| `notion_task` | `notion_crew` | `notion_overhaul` / `notion_capture_crew` |
| `practice_runner_task` | `practice_runner_crew` | (none — missing entirely) |
| `linkedin_x_campaign` | `griot_crew` | `linkedin_x_crew` |
| `voice_polishing` | `voice_crew` | `voice_polisher_crew` |
| `skill_build` | `skill_builder_crew` | (none — missing entirely) |
| `chat` | `chat_crew` | (none — missing entirely) |

- **File:** `orchestrator/router.py` lines 26–175 (TASK_TYPES definitions)
- **File:** `orchestrator/crews.py` lines 2381–2410 (CREW_REGISTRY)
- **Severity:** RED
- **Fix:** Align the `crew` values in `TASK_TYPES` to match exact `CREW_REGISTRY` keys. Quick renames: `web_builder_crew` -> `website_crew`, `app_builder_crew` -> `app_crew`, `griot_crew` -> `linkedin_x_crew`, `voice_crew` -> `voice_polisher_crew`. For `notion_task`, `practice_runner_task`, `skill_build`, and `chat` — either add the missing crew functions to `CREW_REGISTRY` or reroute them to an existing crew.

**11 CREW_REGISTRY entries are unreachable** (no task type in `router.py` routes to them):

- `website_crew` — shadowed by the `web_builder_crew` naming mismatch
- `app_crew` — shadowed by the `app_builder_crew` naming mismatch
- `consulting_crew` — no task type defined
- `linkedin_x_crew` — shadowed by the `griot_crew` naming mismatch
- `notion_overhaul` — shadowed by the `notion_crew` naming mismatch
- `voice_polisher_crew` — shadowed by the `voice_crew` naming mismatch
- `prompt_engineer_crew` — no task type defined
- `3d_website_crew` — no task type defined
- `news_brief_crew` — no task type defined
- `notion_tasks_crew` — no task type defined
- `enrich_leads_crew` — no task type defined

- **Severity:** YELLOW (dead registry entries — no production crash, but crews are inaccessible)
- **Fix:** Either add task types in `router.py` for each orphaned crew, or remove the orphaned entries from `CREW_REGISTRY` if the features were abandoned.

---

### CHECK 5 — CONFIG DRIFT (YELLOW)

**`ALLOWED_USER_IDS` has no entry in `docker-compose.yml` and defaults to `""`.**

- **File:** `orchestrator/orchestrator.py:1205`
- **Behavior:** Empty string means the allowed-user filter passes an empty set. Depending on downstream logic (line 1206+), this may allow all Telegram users to trigger the orchestrator.
- **Severity:** YELLOW (potential security gap)
- **Fix:** Add `ALLOWED_USER_IDS=${ALLOWED_USER_IDS}` to the orchestrator `environment` block in `docker-compose.yml` and set it in `.env`.

**`VERCEL_TOKEN` referenced in code but not in `docker-compose.yml`.**

- **File:** `orchestrator/tools.py:1199`
- **Note:** Vercel MCP integration is currently commented out (lines 1202-1208), so this is not actively breaking anything.
- **Severity:** YELLOW (informational)
- **Fix:** Either add `VERCEL_TOKEN=${VERCEL_TOKEN}` to `docker-compose.yml` when the Vercel integration is re-enabled, or remove the dead reference.

**`N8N_ESCALATION_WEBHOOK` referenced but not in `docker-compose.yml`.**

- **File:** `orchestrator/tools.py:951`, `orchestrator/tools.py:1007`
- **Note:** Both call sites are guarded with `if webhook_url:`, so missing this var degrades escalation silently rather than crashing.
- **Severity:** YELLOW (silent feature degradation)
- **Fix:** Add `N8N_ESCALATION_WEBHOOK=${N8N_ESCALATION_WEBHOOK}` to `docker-compose.yml`.

---

## Checks Passed

- **CHECK 2 — ORPHANED FILES:** Clean. `db.py` (the only non-standard file) is imported by `orchestrator/tools.py` and `orchestrator/scheduler.py`. No orphaned `.py` files found.
- **CHECK 3 — REQUIREMENTS DRIFT:** No missing third-party packages identified. All visible top-level imports map to packages in `requirements.txt`. No obviously unused packages detected at this scan depth.
- **CHECK 4 — DEAD AGENTS:** Clean. `build_news_brief_agent`, `build_gws_agent`, and `build_notion_capture_agent` are not in the top-level import block in `crews.py` but are all called via lazy local imports inside their respective crew builder functions (lines 1591, 1705, 1748, 1810, 1891, 2003). No dead agents.

---

*Report generated automatically by agentsHQ Weekly Health Check. Repo: github.com/bokar83/agentHQ*
