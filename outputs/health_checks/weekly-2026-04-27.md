# agentsHQ Weekly Health Check — 2026-04-27

## Status: YELLOW
Minor issues found — dead code, one missing dependency declaration, orphaned files, and a few missing env vars for optional features. No core routing is broken. Not urgent but clean up recommended this week.

## Issues Found

### Check 1: Router/Registry Mismatch

- **router.py:231** | `chat` task type maps to `chat_crew` | YELLOW | `chat_crew` is not in `CREW_REGISTRY` in crews.py. If the LLM fallback classifies a message as "chat", engine.py calls `assemble_crew("chat_crew")` which silently falls through to `unknown_crew`. Fix: add `"chat_crew": build_chat_crew` to CREW_REGISTRY, or add a pre-assemble check in engine.py for task_type=="chat" that routes to `run_chat()` directly.
- **router.py:290** | `inbound_lead` task type maps to `inbound_lead_direct` | YELLOW | `inbound_lead_direct` is not in `CREW_REGISTRY`. The description says it is webhook-only (not keyword-routed), but the crew name implies it should be in the registry. If ever invoked via `classify_task`, it falls back to `unknown_crew`. Fix: either add it to CREW_REGISTRY or rename the crew value to make the non-registry intent explicit (e.g. `"__webhook_only__"`).
- **crews.py:2978** | `consulting_crew` in CREW_REGISTRY — no task type in router.py routes to it | YELLOW | Dead registry entry. Either add a task type for consulting work or remove the entry.
- **crews.py:2988** | `prompt_engineer_crew` in CREW_REGISTRY — no task type in router.py routes to it | YELLOW | Dead registry entry. Same recommendation.
- **crews.py:2989** | `3d_website_crew` in CREW_REGISTRY — no task type in router.py routes to it | YELLOW | Dead registry entry. Same recommendation.
- **crews.py:2990** | `news_brief_crew` in CREW_REGISTRY — no task type in router.py routes to it | YELLOW | Dead registry entry. Same recommendation.
- **crews.py:2997** | `enrich_leads_crew` in CREW_REGISTRY — no task type in router.py routes to it | YELLOW | Dead registry entry. Likely a planned feature stub. Add a task type or remove until needed.

### Check 2: Orphaned Files

- **orchestrator/content_board_reorder.py** | Zero imports from any other orchestrator file | YELLOW | Possibly superseded by `content_board_fetch_crew` logic. Verify if still needed; delete if not.
- **orchestrator/harvest_reviewer.py** | Zero imports | YELLOW | Harvest roadmap stub. No active crew calls it. Delete or connect.
- **orchestrator/harvest_triage.py** | Zero imports | YELLOW | Same as above.
- **orchestrator/memory_qmd.py** | Zero imports | YELLOW | Not referenced anywhere. Delete if superseded by `episodic_memory.py`.
- **orchestrator/scrub_titles.py** | Zero imports | YELLOW | Utility with no callers. Delete if no longer needed.
- **orchestrator/upscale.py** | Zero imports | YELLOW | No callers. Delete or wire into `media_crew` / `kie_media.py` if still planned.

### Check 3: Requirements Drift

- **orchestrator/app.py:129,146,270** | `import jwt as pyjwt` used in `/chat-token` and JWT-auth middleware | YELLOW | `PyJWT` is not declared in `requirements.txt`. It may be a transitive dep (e.g. via `python-jose` or `crewai`), but it is not guaranteed. If absent, the `/chat-token` endpoint and chat-UI auth will fail with `ModuleNotFoundError` at call time (not startup). Fix: add `PyJWT>=2.8.0` to `requirements.txt`.

### Check 4: Dead Agents

No issues found. All 20 `build_*_agent()` functions defined in `agents.py` are called by at least one crew.

### Check 5: Config Drift

- **orchestrator/tools.py:1357** | `VERCEL_TOKEN = os.environ.get("VERCEL_TOKEN")` — not in docker-compose.yml environment block | YELLOW | No default. Vercel crew operations will silently fail with None token. Fix: add `- VERCEL_TOKEN=${VERCEL_TOKEN}` to docker-compose.yml.
- **orchestrator/kie_media.py:339** | `os.environ.get("GWS_CREDS_PATH")` — not in docker-compose.yml | YELLOW | Returns None. GWS credential path used for media operations will be unset in container. Fix: add `- GWS_CREDS_PATH=${GWS_CREDS_PATH}` to docker-compose.yml.
- **orchestrator/blotato_publisher.py + auto_publisher.py** | `BLOTATO_YOUTUBE_ACCOUNT_ID` — not in docker-compose.yml | YELLOW | Compose has `BLOTATO_YT_BAOBAB_ACCOUNT_ID` and `BLOTATO_YT_CATALYST_ACCOUNT_ID` but not the generic `BLOTATO_YOUTUBE_ACCOUNT_ID` form referenced in code. YouTube publishing via the generic account will fail silently. Verify which env var name the code actually requires and align compose accordingly.
- **orchestrator/notifier.py:31-32** | `SMTP_HOST` and `SMTP_PORT` — not in docker-compose.yml | YELLOW | Both have safe defaults (`smtp.gmail.com`, `587`), so email will work if the defaults match the actual SMTP provider. But if a different provider is needed, there is no override path from docker-compose. Fix: add both to docker-compose.yml for explicit control.

## Checks Passed

- **Check 1 (partial)**: All 32 task types in `TASK_TYPES` that are expected to route through `assemble_crew` have their crew values present in `CREW_REGISTRY` (aside from `chat_crew` and `inbound_lead_direct` noted above).
- **Check 3 (partial)**: All other third-party packages in `requirements.txt` have matching imports across `orchestrator/*.py`. No unused packages detected in the core framework, API, LLM, or database sections.
- **Check 4**: No dead agents. All 20 `build_*_agent()` functions are called by at least one crew in `crews.py`.
- **Check 5 (partial)**: All critical API keys and service credentials are present in docker-compose.yml — `OPENROUTER_API_KEY`, `ANTHROPIC_API_KEY`, `FIRECRAWL_API_KEY`, `ORCHESTRATOR_API_KEY`, `HEALTH_REPORT_TOKEN`, `NOTION_SECRET`, `BLOTATO_API_KEY`, all Supabase vars, all Telegram vars, all Notion DB IDs used by active crews. `SMTP_USER` and `SMTP_PASS` are present.

---
Report ends here.
