# agentsHQ Weekly Health Check — 2026-05-04

## Status: YELLOW

Minor issues only. No broken routing, no missing required dependencies, no logic bugs that would crash active flows. All issues below are dead code or incomplete env var declarations for optional/new features.

---

## Issues Found

### 1. ROUTER/REGISTRY MISMATCH — 5 unreachable crews (YELLOW)

The following entries exist in `CREW_REGISTRY` in `orchestrator/crews.py:3293` but have **no corresponding task type** in `router.py` that maps to them. They cannot be reached via normal message routing; `assemble_crew()` will never select them unless explicitly called with a hardcoded crew_type string.

| CREW_REGISTRY Key | File | Severity |
|---|---|---|
| `consulting_crew` | crews.py:3295 | YELLOW |
| `prompt_engineer_crew` | crews.py:3307 | YELLOW |
| `3d_website_crew` | crews.py:3308 | YELLOW |
| `news_brief_crew` | crews.py:3309 | YELLOW |
| `enrich_leads_crew` | crews.py:3316 | YELLOW |

**Note on `chat_crew` and `inbound_lead_direct`**: Both appear in router.py but not in CREW_REGISTRY. This is intentional — `chat` is intercepted in `handlers.py:198` and routed to `run_chat_with_buttons()` before crew assembly; `inbound_lead_direct` is handled entirely via `skills/inbound_lead/runner.py` in `app.py:688`. Neither is a bug.

**Recommended fix**: Either add task types in router.py for the 5 unreachable crews (e.g. `"news_brief"` -> `news_brief_crew`), or archive the crew functions if the feature is descoped.

---

### 2. ORPHANED FILES — 3 files with zero imports (YELLOW)

The following files in `orchestrator/` are not imported by any other module. They have no import references from any `.py`, `.yml`, `.sh`, or `.json` file in the active codebase.

| File | Note | Severity |
|---|---|---|
| `orchestrator/memory_qmd.py` | "Local semantic search via QMD" utility; no callers found | YELLOW |
| `orchestrator/scrub_titles.py` | Listed in inventory JSON; zero code imports | YELLOW |
| `orchestrator/upscale.py` | fal.ai upscale helper; zero imports from other orchestrator files | YELLOW |

**Not flagged** (intentionally standalone CLI scripts): `content_board_reorder.py` (invoked via `docker exec`; referenced in griot.py comment), `harvest_reviewer.py` (batch CLI with in-file usage example).

**Recommended fix**: Verify each against production logs. If unused for 14+ days, archive to `zzzArchive/` per folder governance rules.

---

### 3. REQUIREMENTS DRIFT — packages in requirements.txt with no direct imports (YELLOW)

The following packages in `orchestrator/requirements.txt` have **zero direct import statements** across all `orchestrator/*.py` files. They may be transitive dependencies of `crewai-tools` or other packages, but should be verified.

| Package | Possible reason | Severity |
|---|---|---|
| `beautifulsoup4` | May be used by crewai-tools internally | YELLOW |
| `playwright` | May be used by crewai-tools browser tools | YELLOW |
| `patchright` | Paired with playwright; no direct use found | YELLOW |
| `curl_cffi` | HTTP client alt; no direct use found | YELLOW |
| `tenacity` | Retry library; may be used by crewai internally | YELLOW |
| `python-dotenv` | Likely used at container startup, not in orchestrator code | YELLOW |
| `pypdf` | PDF parsing; may be used by crewai-tools | YELLOW |

**Recommended fix**: Run `pip check` in the container to confirm no conflicts. Pin versions are already in place; no immediate action needed unless a Docker image size audit is warranted.

---

### 4. CONFIG DRIFT — env vars referenced in code but missing from docker-compose.yml (YELLOW)

The following env vars are read via `os.environ.get()` in active orchestrator code but have **no entry in the `orchestrator:` service `environment:` block** of `docker-compose.yml`. Per container deploy protocol, vars missing from `docker-compose.yml` silently receive no value inside the container even when set in `.env`.

**Missing API keys (no defaults — features will silently fail):**

| Var | File:Line | Impact |
|---|---|---|
| `VERCEL_TOKEN` | `tools.py:1357` | Vercel task crew returns None on init; `vercel_task` / `github_task` deploy checks broken |
| `APIFY_API_TOKEN` | `niche_research.py:192,314` | Niche research Apify path silently skipped |
| `ELEVENLABS_API_KEY` | `studio_voice_generator.py:103` | Voice generation produces empty audio; Studio voice crew fails silently |

**Missing social platform IDs (auto_publisher.py:210-227; all return None):**

These 7 Blotato platform account IDs are referenced in `auto_publisher.py` but not declared in docker-compose.yml. Publishing to these platforms via the auto-publisher will silently no-op:

- `BLOTATO_FACEBOOK_ACCOUNT_ID`
- `BLOTATO_IG_ACCOUNT_ID` / `BLOTATO_INSTAGRAM_ACCOUNT_ID`
- `BLOTATO_TIKTOK_ACCOUNT_ID`
- `BLOTATO_THREADS_ACCOUNT_ID`
- `BLOTATO_YOUTUBE_ACCOUNT_ID`
- `BLOTATO_PINTEREST_ACCOUNT_ID`
- `BLOTATO_BLUESKY_ACCOUNT_ID`

**Not flagged** (have hardcoded defaults in code): `SMTP_HOST` (defaults to smtp.gmail.com), `SMTP_PORT` (587), `NOTION_CRM_LEADS_DB_ID` (has fallback UUID), `CONTENT_DRIVE_FOLDER_ID` (has fallback folder ID), `FAL_API_KEY` (defaults to empty string with graceful fail).

**Recommended fix**: Add each missing var to the `orchestrator:` `environment:` block in `docker-compose.yml`. Rebuild container (`scripts/orc_rebuild.sh`) to pick up changes. If a platform is not yet active (Facebook, Pinterest, Bluesky, TikTok), add placeholders with empty values so the intent is documented.

---

## Checks Passed

- **Router integrity**: All task types in `router.py` that reach `assemble_crew()` have valid entries in `CREW_REGISTRY`. `chat` and `inbound_lead_direct` correctly bypass crew assembly.
- **Dead agents**: All 24 `build_*_agent()` functions in `agents.py` are called by at least one crew in `crews.py` (several via inline local imports within crew functions — confirmed at lines 2308, 2422, 2497, 2562, 2643, 2755).
- **Core routing logic**: Keyword fast-path, LLM fallback, content board signals, and read-intent CRM shortcut are structurally intact in `router.py`.
- **Docker networking**: Postgres, Qdrant, Traefik, and orc-crewai service wiring in `docker-compose.yml` is consistent with known architecture.
- **No stdlib/third-party confusion**: All direct imports in orchestrator code that are third-party packages are covered by `requirements.txt`.
- **No circular imports detected**: Import graph for core modules (engine, crews, agents, router, handlers, tools) is acyclic based on import declarations.

---

*Report generated: 2026-05-04 | Checks: Router/Registry, Orphaned Files, Requirements Drift, Dead Agents, Config Drift*
