# agentsHQ Weekly Health Check — 2026-04-13

## Status: RED

RED = broken routing detected. Eight task types route to crew names that do not exist in CREW_REGISTRY. All eight fall back silently to `build_unknown_crew`. Affected features are broken in production until router.py crew names are corrected.

---

## Issues Found

### CHECK 1 — ROUTER/REGISTRY MISMATCH (RED)

**8 task types route to crews that do not exist in CREW_REGISTRY:**

| Task Type | Router crew value | Closest CREW_REGISTRY key | Impact |
|---|---|---|---|
| `website_build` | `web_builder_crew` | `website_crew` | All website build requests fall back to unknown_crew |
| `app_build` | `app_builder_crew` | `app_crew` | All app build requests fall back to unknown_crew |
| `notion_task` | `notion_crew` | `notion_overhaul` | All Notion tasks fall back to unknown_crew |
| `practice_runner_task` | `practice_runner_crew` | _(none)_ | Practice Runner broken entirely |
| `linkedin_x_campaign` | `griot_crew` | `linkedin_x_crew` | All LinkedIn/X campaigns fall back to unknown_crew |
| `voice_polishing` | `voice_crew` | `voice_polisher_crew` | All voice polish requests fall back to unknown_crew |
| `skill_build` | `skill_builder_crew` | _(none)_ | Skill Builder broken entirely |
| `chat` | `chat_crew` | _(none)_ | All general chat falls back to unknown_crew |

**Fix:** In `orchestrator/router.py`, update the `crew` value for each affected task type to match the correct key in `CREW_REGISTRY`:
- `web_builder_crew` -> `website_crew`
- `app_builder_crew` -> `app_crew`
- `griot_crew` -> `linkedin_x_crew`
- `voice_crew` -> `voice_polisher_crew`
- `notion_crew`: confirm whether `notion_overhaul` is the intended crew or if a new `notion_crew` needs to be added
- `practice_runner_crew` and `skill_builder_crew` and `chat_crew`: either add missing crews to CREW_REGISTRY or remove the task types from router.py

**14 CREW_REGISTRY entries are unreachable via router.py** (dead registry entries, YELLOW):
`website_crew`, `app_crew`, `consulting_crew`, `linkedin_x_crew`, `notion_overhaul`, `agent_creator_crew`, `voice_polisher_crew`, `prompt_engineer_crew`, `3d_website_crew`, `news_brief_crew`, `notion_tasks_crew`, `crm_outreach_crew`, `mark_outreach_sent_crew`, `enrich_leads_crew`

These crews exist and build successfully but cannot be triggered through the router. They are only reachable via direct API calls. This is a YELLOW issue on its own, but is related to the RED mismatches above.

---

### CHECK 4 — DEAD AGENT (YELLOW)

- `orchestrator/agents.py:633` — `build_content_reviewer_agent()` is defined and imported in `orchestrator/crews.py:49` but is never called by any crew builder function.
- Severity: YELLOW — dead code, no functional impact. Either wire it into `content_review_crew` or remove the import and definition.

---

### CHECK 3 — REQUIREMENTS DRIFT (YELLOW)

The following packages are in `orchestrator/requirements.txt` but are never directly imported in any `orchestrator/*.py` file:

- `tenacity` — no direct import found; likely a transitive dep of crewai (which uses it for retries)
- `pypdf` — no direct import found; likely a transitive dep of crewai-tools (PDF tool support)
- `curl_cffi` — no direct import found; likely a transitive dep of crewai-tools (web scraping)

Severity: YELLOW — these are likely intentional explicit pins for transitive deps. No action needed unless a dep audit confirms they are redundant.

---

### CHECK 5 — CONFIG DRIFT (YELLOW)

- `orchestrator/tools.py:1196` — `VERCEL_TOKEN = os.environ.get("VERCEL_TOKEN")` has no default value and no entry in `docker-compose.yml`. If a `vercel_task` is routed to `code_crew`, any tool that reads `VERCEL_TOKEN` will receive `None` and fail silently.
- Severity: YELLOW — add `VERCEL_TOKEN=${VERCEL_TOKEN}` to the orchestrator environment block in `docker-compose.yml`, or explicitly handle the `None` case in tools.py.

---

## Checks Passed

- **Check 2 (Orphaned Files):** `orchestrator/db.py` is not in the standard file list but is imported by `orchestrator/scheduler.py` (lines 306, 323, 392). Not orphaned. All other `.py` files in `orchestrator/` are either in the expected list or imported. Clean.
- **Check 3 (partial):** All packages in `requirements.txt` that are directly imported in code are accounted for. No missing third-party packages detected.
- **Check 4 (partial):** 22 of 23 defined agent functions are called by at least one crew. Only `build_content_reviewer_agent` is unused.
- **Check 5 (partial):** All required env vars with no fallback defaults that are actively used in routing logic (`OPENROUTER_API_KEY`, `POSTGRES_PASSWORD`, `NOTION_SECRET`, `TELEGRAM_CHAT_ID`, `ORCHESTRATOR_TELEGRAM_BOT_TOKEN`, `GITHUB_TOKEN`, `HEALTH_REPORT_TOKEN`, `ORCHESTRATOR_API_KEY`) are declared in `docker-compose.yml`.

---

_Report generated automatically by agentsHQ weekly health check — 2026-04-13_
