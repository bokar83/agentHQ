# agentsHQ Weekly Health Check — 2026-04-07

## Status: YELLOW
- YELLOW = minor issues (dead code, missing docs) — not urgent

## Issues Found

### Config Drift (Check 5)

- **`orchestrator/tools.py:938`** | `VERCEL_TOKEN` | YELLOW
  - `os.environ.get("VERCEL_TOKEN")` — referenced but **not declared in docker-compose.yml**.
  - Resolves to `None` silently; Vercel deployment features will fail at runtime if triggered.
  - **Fix:** Add `VERCEL_TOKEN=${VERCEL_TOKEN}` to the `orchestrator` environment block in `docker-compose.yml` and supply the value in `.env`.

- **`orchestrator/tools.py:690,746`** | `N8N_ESCALATION_WEBHOOK` | YELLOW
  - `os.environ.get("N8N_ESCALATION_WEBHOOK")` — referenced but **not declared in docker-compose.yml**.
  - Escalation webhook calls will silently no-op. May not be in use yet, but the code path exists.
  - **Fix:** Add `N8N_ESCALATION_WEBHOOK=${N8N_ESCALATION_WEBHOOK}` to docker-compose.yml and set the value in `.env` when the escalation feature is activated.

---

## Checks Passed

- **Check 1 — Router/Registry Mismatch:** All 18 task types in `router.py` map to valid `CREW_REGISTRY` entries in `crews.py`. `unknown_crew` in registry is intentional fallback — not a leak.
- **Check 2 — Orphaned Files:** `__init__.py` is an empty package marker (normal). `db.py` is imported by `scheduler.py` — not orphaned.
- **Check 3 — Requirements Drift:** All third-party packages imported directly in `orchestrator/*.py` are present in `requirements.txt`. Packages in requirements not directly imported (`litellm`, `qdrant-client`, `mcp`, `tenacity`, `python-dotenv`) are legitimate transitive dependencies required by crewai internals.
- **Check 4 — Dead Agents:** All 21 `build_*_agent()` functions in `agents.py` are called by at least one crew in `crews.py`. No dead agent functions.
- **Check 5 — Config Drift (partial):** All primary operational vars (`OPENROUTER_API_KEY`, `SERPER_API_KEY`, `FIRECRAWL_API_KEY`, Postgres, Qdrant, Telegram, Google, Supabase, Notion, Forge, Apollo, Hunter, SMTP, GitHub) are declared in docker-compose.yml. Only `VERCEL_TOKEN` and `N8N_ESCALATION_WEBHOOK` are missing (see Issues above).

---
