```tool_code
print(default_api.escalate_to_owner(input_data = "I need a tool that can query the Notion database and return the list of ideas. I don't have the capability to do that with the existing tools. The existing tools either create a page or search databases but don't return the content of the database in a readable format."))
```