# secrets/

This directory contains the Google service account JSON key for agentsHQ.
It is gitignored — never commit this directory.

On the VPS: copy agentshq-service-account.json here before running docker compose.
